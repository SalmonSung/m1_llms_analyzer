# Runtime dependencies.
#
# NOTE: torch is intentionally NOT pinned to an exact version. Google Colab ships
# a CUDA-matched torch build; forcing a different version triggers a multi-minute
# reinstall and can break CUDA. The notebook installs with
# `--upgrade-strategy only-if-needed` so an existing satisfying torch is kept.
torch>=2.0
transformers>=4.51        # Qwen3 support
numpy>=1.24
huggingface_hub>=0.23
accelerate>=0.26       # ModelConfig(device_map=...) for the 20B/31B models of Task 8a
tqdm>=4.65
# Experiments (notebooks/experiment_1b.ipynb): treebank, figures, bootstrap stats.
nltk>=3.8
matplotlib>=3.7
scipy>=1.10
# Experiment 9a (notebooks/experiment_9a.ipynb): streams Wikipedia paragraphs.
datasets>=2.14
